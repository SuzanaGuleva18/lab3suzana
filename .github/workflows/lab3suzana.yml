name: Lab 3 Workflow

on: push

jobs:
  build:
    name: Job 1
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        uses: actions@v4
      - name: Step 2
        run: ls -la
      - name: Step 3
        run: pwd
